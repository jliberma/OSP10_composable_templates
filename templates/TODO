x. Create new vm disk of custom type
x. import new vm disk
x. configure new profile
x. add new profile to new vm
x. modify networking and CPU for new compute type
x. deploy
x. deploy and access test instances
x. set overcommit ratios for one type of host versus another
x. check scheduler default filters
x. configure aggregate ram filter
x. create host aggregates and availability zones
x. test aggregate ram filter
o. Re-assign a node from devel to perf aggregate and see if it gets instances scheduled by perf core_filter
o. Output which nodes are assigned to which hosts after first instance creation loop (should be perf only)
o. Remove aggregate filter and re-run test
o. add to script to capture filter setting from plan
